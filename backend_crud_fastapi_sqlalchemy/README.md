# Backend of a Bookshop CRUD app using PostgreSQL with FastAPI and SQLAlchemy

## Quick start
```bash
$ pip install -r requirements.txt
$ cd backend_crud_fastapi_sqlalchemy/app
$ uvicorn main:app --reload
```
